<h1><%= t '.title' %></h1>

<table>
  <thead>
    <tr>
      <th><%= t 'table.header.id' %></th>
      <th><%= t 'table.header.name' %></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @categories.each do |category| %>
    <tr>
      <td><%= category.id %></td>
      <td><%= category.name %></td>
      <td><%= link_to (t '.action.show'), category_suppliers_url(category)%></td>
    </tr>
  <% end %>
  </tbody>
</table>

